%p= "#{t(:hello)} #{@email}"

- if @resource.try(:unconfirmed_email?)
  %p= "#{t(:notify_being_changed_email)} #{@resource.unconfirmed_email}"
- else
  %p= "#{t(:notify_changed_email)} #{@resource.email}"

