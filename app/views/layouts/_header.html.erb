<div class="media-btn" data-controller="side-menu">
  <button class="openbtn-media" data-action="click->side-menu#sideMenuToggleMedia">☰</button>
</div>

<div class="flex-grow-1"></div>

<div class="flex flex-column float-end">
  <div class="flex-row-reverse">
    <%= render 'shared/change_locale' %>
  </div>
  <div class="d-flex px-2" >
    <% if current_user %>
      <%= current_user.email %>
      <%= button_to t(:sign_out), destroy_user_session_path, method: :delete %>
    <% else %>
      <%= link_to t(:sign_up), new_user_registration_path %>
      <%= link_to t(:login), new_user_session_path %>
    <% end %>

  </div>
</div>

